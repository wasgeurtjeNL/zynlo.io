{
  "framework": "nextjs",
  "buildCommand": "cd ../.. && pnpm turbo run build --filter=@zynlo/dashboard",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": ".next",
  "rootDirectory": "apps/dashboard"
}
